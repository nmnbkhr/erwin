# Raw captured artefacts — regenerated by every capture.mjs run, kept only for
# eyeballing the actual PDF/CSV/Markdown. The reviewable record is the JSON in
# baseline/, which is committed. Raw PDFs are not diffable and must not be.
raw/
